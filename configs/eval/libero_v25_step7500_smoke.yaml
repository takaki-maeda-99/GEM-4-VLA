# Fast closed-loop smoke for the v25 baseline oracle.
# Uses the known-good step_7500 checkpoint but evaluates only task 0 once.
seed: 7
checkpoint:
  path: outputs/libero_spatial_v25/checkpoints/step_7500
model:
  num_domains: 1
  compat_profile: vla_gemma4_baseline
  hidden_dim: 1536
  num_blocks: 24
  use_grad_checkpoint: false
  use_wrist_bridge: true
  ungated_streams: false
  use_soft_prompt: false
  prompt_max_len: 20
  use_baseline_projectors: true
  baseline_scene_init_proj_dim: 8192
  vision_placeholder_mode: unused_range
  freeze_llm_and_aq: true
vision:
  type: timm
  model_name: google/siglip-so400m-patch14-224
language: {model_name: google/gemma-4-E2B}
data:
  unnorm_key: libero_spatial_no_noops
  stats_path: /misc/dl00/takaki/vla-gemma-4/VLA-Adapter/outputs/LIBERO-Spatial-Pro/dataset_statistics.json
robot:
  bddl_path_root: /misc/dl00/takaki/vla-gemma-4/LIBERO/libero/libero/bddl_files
  task_suite: libero_spatial
  image_size: 256
  libero_path: ${oc.env:LIBERO_PATH,/misc/dl00/takaki/vla-gemma-4/LIBERO}
eval:
  task_idxs: [0]
  num_episodes_per_task: 1
  max_steps: 210
  num_steps_wait: 10
  video_dir: null
  video_ext: mp4
  video_fps: 10
